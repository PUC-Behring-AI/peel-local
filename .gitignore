__pycache__/
*.pyc

.ipynb_checkpoints/

# GlossBERT is now fetched from the Hugging Face Hub (jvomiranda/GlossBERT_Checkpoint);
# a local checkpoint folder is no longer required. Ignored defensively in case one is
# kept around for offline use.
GlossBERT_Checkpoint/
**/GlossBERT_Checkpoint/

.DS_Store
Thumbs.db

# Pipeline-graph generator tooling (PEEL-Local-pipeline-overview.png/.pdf and
# PEEL-Local-pipeline-full.html at the repo root are the committed deliverables;
# the code/data/vendored binaries that produce them are not).
docgraph/
